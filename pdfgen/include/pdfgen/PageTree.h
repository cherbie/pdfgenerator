// PDF "Page Tree"
// The pages of a document are accessed through a structure known as the
// "page tree", which defines their ordering within the document.
class PageTree
{
	protected:
		PageTree();
		~PageTree();
}
